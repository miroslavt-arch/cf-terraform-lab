{
  "//": "Known-GOOD fixture. conftest MUST pass. A test resource and a tagged resource.",
  "resource_changes": [
    { "address": "cloudflare_dns_record.probe", "type": "cloudflare_dns_record",
      "change": { "actions": ["delete"], "before": { "name": "tftest-probe" }, "after": null } },
    { "address": "cloudflare_dns_record.preview", "type": "cloudflare_dns_record",
      "change": { "actions": ["delete"], "before": { "name": "old-preview", "tags": { "pipeline_may_destroy": "true" } }, "after": null } }
  ]
}
